/**
 * Config Command — placeholder.
 *
 * The real implementation lands in subsequent tasks (main menu + OAuth submenu,
 * API keys submenu, Services submenu, reorder fallbacks sub-flow). This stub
 * exists so `init.ts` can be developed and tested in advance.
 */

export async function config(): Promise<void> {
  // Real implementation forthcoming. The init command imports this and invokes
  // it on a fresh install; until the real menu lands, calling system2 config
  // (or finishing init's chain) is a no-op.
}
